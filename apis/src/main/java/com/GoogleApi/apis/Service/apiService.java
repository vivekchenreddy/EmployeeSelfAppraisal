package com.GoogleApi.apis.Service;

import org.springframework.stereotype.Service;

@Service
public interface apiService {
   // public String sum(IMPACT value);
    public int set_code(String b);
}
